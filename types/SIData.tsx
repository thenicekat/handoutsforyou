export type SI_Company = {
    name: string,
    eligibility: string
    cgpa_cutoff: string,
    roles: string,
    stipend: string,
    selects: string,
    selection_rounds: string,
    year: string
}